Algoritmo "descontoProgressivo"
// Disciplina   : [Linguagem e Lógica de Programação]
// Professor   : Antonio Carlos Nicolodi 
// Descrição   : Aqui você descreve o que o programa faz! (função)
// Autor(a)    : Nome do(a) aluno(a)
// Data atual  : 30/08/2023
Var
// Seção de Declarações das variáveis 
    valorCompra, valorComDesconto: Real

Inicio
// Seção de Comandos, procedimento, funções, operadores, etc... 

   Escreva("Digite o valor total da compra:R$ ")
   Leia(valorCompra)

   Se valorCompra <= 1000 Então
      valorComDesconto <- (valorCompra - (valorCompra * 20/100))
      
   Senão
      Se valorCompra <= 2000 Então
         valorComDesconto <- (valorCompra - (valorCompra * 15/100))
         
      Senão
         valorComDesconto <- (valorCompra - (valorCompra * 10/100))
         
      FimSe
   FimSe

   Escreva("O valor com desconto é:R$ ", valorComDesconto)



Fimalgoritmo
